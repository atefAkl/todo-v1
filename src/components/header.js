import React from "react";
import Brand from "./brand";

const Header = () => {
  return (
    <header>
      <div className="container">
        <Brand />
      </div>
    </header>
  );
};

export default Header;
